node_modules/
dist/
*.log

# Secrets — never commit
.env
.env.*
!.env.example

# Local wallet files — never commit
wallets.txt
wallets.secure
*.secure
vaults/

# Private keys — never commit
wallets/
keys.txt
*.key
